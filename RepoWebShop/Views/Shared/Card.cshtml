@model string


@{var title = String.IsNullOrEmpty(Model) ? "De las Artes" : Model; }

<section class="container" id="card">
    <div class="rectangle">
        <h1>@title</h1>
    </div>
    <div class="triangle-l"></div>
    <div class="content">
    </div>
</section>